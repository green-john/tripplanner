import { inject } from '../lib/aurelia-framework';
import { SerializerService } from '../utils/serializer.service';

@inject()
export class LoginService {
    constructor() {

    }

    authenticate(username, password) {

    }
}
